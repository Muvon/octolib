# Pre-commit configuration for Rust project
repos:
  # Rust-specific hooks
  - repo: https://github.com/doublify/pre-commit-rust
    rev: v1.0
    hooks:
      # Format code with cargo fmt
      - id: fmt
        name: cargo fmt
        description: Format Rust code with cargo fmt
        entry: cargo fmt
        language: system
        files: \.rs$
        args: ['--all', '--']

      # Run cargo clippy for linting
      # Note: cuda/cudnn/mkl excluded — hardware-specific, require native toolchains
      - id: clippy
        name: cargo clippy
        description: Lint Rust code with cargo clippy
        entry: cargo clippy
        language: system
        files: \.rs$
        args: ['--all-targets', '--features', 'fastembed,huggingface,metal,accelerate', '--', '-D', 'warnings']

      # Check that code compiles
      # Note: cuda/cudnn/mkl excluded — hardware-specific, require native toolchains
      - id: cargo-check
        name: cargo check
        description: Check that Rust code compiles
        entry: cargo check
        language: system
        files: \.rs$
        args: ['--all-targets', '--features', 'fastembed,huggingface,metal,accelerate']

  # Local hooks for cargo doc
  - repo: local
    hooks:
      # Check documentation builds without warnings
      - id: rust-doc
        name: cargo doc
        description: Check Rust documentation builds without warnings
        entry: bash
        language: system
        args: ['-c', 'RUSTDOCFLAGS="-D warnings" cargo doc --features fastembed,huggingface,metal,accelerate --no-deps']
        pass_filenames: false
        always_run: true
        types: [rust]

  # General hooks
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      # Remove trailing whitespace
      - id: trailing-whitespace

      # Ensure files end with newline
      - id: end-of-file-fixer

      # Check for merge conflicts
      - id: check-merge-conflict

      # Check YAML syntax
      - id: check-yaml

      # Check TOML syntax
      - id: check-toml

      # Prevent large files from being committed
      - id: check-added-large-files
        args: ['--maxkb=1000']
